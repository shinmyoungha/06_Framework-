<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.kh.project.admin.model.mapper.AdminMapper">

	<select id="login">
		SELECT MEMBER_NO, MEMBER_EMAIL, MEMBER_NICKNAME,
		MEMBER_PW,
		AUTHORITY FROM "MEMBER"
		WHERE MEMBER_EMAIL = #{memberEmail}
		AND MEMBER_DEL_FL = 'N'
		AND AUTHORITY = 2
	</select>

	<!-- 최대 조회수 게시글 조회 만약 최대 조회수가 똑같은 게시물이 여러개 존재한다면 가장 최근에 작성된 게시글 기준(BOARD_NO가 
		큰 순서)으로 정렬해서 하나만 조회 -->
		
	<!-- 가장 조회수 많은 게시글 -->
	<select id="maxReadCount">
		SELECT BT.BOARD_NAME, B.BOARD_NO, COUNT(*) AS
		LIKE_COUNT,
		B.BOARD_TITLE, M.MEMBER_NICKNAME
		FROM "BOARD_LIKE" BL
		JOIN
		"BOARD" B ON
		BL.BOARD_NO = B.BOARD_NO
		JOIN "BOARD_TYPE" BT ON
		B.BOARD_CODE =
		BT.BOARD_CODE
		JOIN "MEMBER" M ON B.MEMBER_NO =
		M.MEMBER_NO
		GROUP BY
		BT.BOARD_NAME, B.BOARD_NO, B.BOARD_TITLE,
		M.MEMBER_NICKNAME
		ORDER BY
		LIKE_COUNT DESC, B.BOARD_NO DESC
		FETCH FIRST 1
		ROWS ONLY
	</select>
	<!-- 가장 좋아요 많은 게시글 -->
	<select id="maxLikeCount">
		SELECT BT.BOARD_NAME, B.BOARD_NO, COUNT(*) AS
		LIKE_COUNT,
		B.BOARD_TITLE, M.MEMBER_NICKNAME
		FROM "BOARD_LIKE" BL
		JOIN
		"BOARD" B ON BL.BOARD_NO = B.BOARD_NO
		JOIN "BOARD_TYPE" BT ON
		B.BOARD_CODE = BT.BOARD_CODE
		JOIN "MEMBER" M ON B.MEMBER_NO =
		M.MEMBER_NO
		GROUP BY BT.BOARD_NAME, B.BOARD_NO, B.BOARD_TITLE,
		M.MEMBER_NICKNAME
		ORDER BY LIKE_COUNT DESC, B.BOARD_NO DESC
		FETCH FIRST
		1 ROWS ONLY
	</select>

	<!-- 가장 댓글 많은 게시글 -->
	<select id="maxCommentCount">
		SELECT BT.BOARD_NAME, B.BOARD_NO, COUNT(*) AS
		COMMENT_COUNT, B.BOARD_TITLE,
		M.MEMBER_NICKNAME
		FROM "COMMENT" C
		JOIN
		"BOARD" B ON B.BOARD_NO =
		C.BOARD_NO
		JOIN "BOARD_TYPE" BT ON
		B.BOARD_CODE = BT.BOARD_CODE
		JOIN
		"MEMBER" M ON B.MEMBER_NO =
		M.MEMBER_NO
		GROUP BY BT.BOARD_NAME,
		B.BOARD_NO, B.BOARD_TITLE,
		M.MEMBER_NICKNAME
		ORDER BY COMMENT_COUNT
		DESC, BOARD_NO DESC
		FETCH FIRST
		1 ROWS ONLY
	</select>
	
</mapper>